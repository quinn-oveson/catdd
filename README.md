1. Make a new virtual environment
``python -m venv .catddvenv``
2. Activate it
``source .catddvenv/bin/activate``
3. Run the dependencies.bash file to install necessary packages
``bash dependencies.bash``